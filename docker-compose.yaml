version: '3.8'

services:

  backend:
    build: ./backend
    restart: always
    volumes:
      - static_volume:/app/staticfiles

  nginx:
    build: .
    ports:
      - "80:80"
    depends_on:
      - backend
    volumes:
      - static_volume:/app/staticfiles

volumes:
  static_volume: